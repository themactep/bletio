[package]
name = "bletio-cli"
version = "0.1.0"
edition = "2021"
description = "Command-line BLE scanner using raw HCI"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
clap = { version = "4", features = ["derive"] }
libc = "0.2"
